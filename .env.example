# Local dev — serves DATs from a local FFXI file server
# Start the server first:
#   python -m http.server 3005   (from your FFXI game directory)
#   docker compose -f docker-compose.dev.yml up -d   (alternative)
NUXT_PUBLIC_DAT_BASE_URL=http://localhost:3005

# To use Cloudflare R2 instead (no local FFXI install needed):
# 1. Copy .env to .env.r2
# 2. Set the two values below in .env.r2:
#      NUXT_PUBLIC_DAT_BASE_URL=https://dats.phoenix-xi.com
#      NUXT_PUBLIC_DAT_ACCESS_TOKEN=<your-token>
# 3. Run: pnpm dev:r2
